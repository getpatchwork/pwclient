---
version: 2
sphinx:
  configuration: docs/conf.py
python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
build:
  os: "ubuntu-24.04"
  tools:
    python: "3.14"
  jobs:
    post_checkout:
      - git fetch --unshallow
